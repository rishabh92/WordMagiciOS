App works well on simulator Iphone 6.

You need to install cocoapods to run the app.

Steps:

Go inside the project folder through the terminal.
Run command "sudo gem install -n /usr/local/bin cocoapods"

Then run command "pod install"

Open the xcworkspace file (not the xcodeproj file.)